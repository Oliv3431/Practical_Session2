#include <stdio.h>

int main() {
    // Use a for loop to iterate from 1 to 100
    for (int i = 1; i <= 100; i++) {
        printf("%d ", i); // Print the value of the variable i
    }

    return 0;
}
